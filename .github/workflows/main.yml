name: Build and Test
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Build C/C++ program
      run: |
        g++ helloworld.c -o helloworld
        ./helloworld
